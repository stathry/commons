<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Copyright https://github.com/stathry/generator All rights reserved. -->
<!-- Auto-generated by FreeMarker on 2018-07-20 10:26 -->
<mapper namespace="org.stathry.commons.dao.ActorDAO">
    <resultMap id="BaseResultMap" type="Actor">
        <result column="actor_id" jdbcType="SMALLINT" property="actorId" />
        <result column="first_name" jdbcType="VARCHAR" property="firstName" />
        <result column="last_name" jdbcType="VARCHAR" property="lastName" />
        <result column="last_update" jdbcType="TIMESTAMP" property="lastUpdate" />
    </resultMap>

    <sql id="Base_Column_List">
      actor_id, first_name, last_name, last_update
    </sql>

    <insert id="insert" useGeneratedKeys="true" parameterType="Actor" keyProperty="id" keyColumn="id">
        insert into actor (actor_id, first_name, last_name, last_update)
        values ( #{actorId, jdbcType=SMALLINT}, #{firstName, jdbcType=VARCHAR}, #{lastName, jdbcType=VARCHAR}, #{lastUpdate, jdbcType=TIMESTAMP} )
    </insert>

    <delete id="deleteById" parameterType="long">
        delete from actor
        where id = #{id, jdbcType=BIGINT}
    </delete>

    <update id="updateById" parameterType="long">
        update actor
        set actor_id = #{actorId, jdbcType=SMALLINT}, first_name = #{firstName, jdbcType=VARCHAR}, last_name = #{lastName, jdbcType=VARCHAR}, last_update = #{lastUpdate, jdbcType=TIMESTAMP}
        where id = #{id, jdbcType=BIGINT}
    </update>

    <select id="queryById" parameterType="long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from actor
        where id = #{id, jdbcType=BIGINT}
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from actor
    </select>

    <select id="count" resultType="long">
        select count(*)
        from actor
    </select>

</mapper>